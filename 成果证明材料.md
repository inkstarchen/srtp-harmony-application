# DayNote 智能笔记系统 — 成果证明材料

## 一、项目概述

**DayNote** 是一款基于 HarmonyOS 6.0.2 平台开发的现代化智能笔记应用，采用 **ArkTS + C++ 混合架构**，集成了 3D 场景渲染、自研物理引擎、富文本编辑、主题系统等核心功能。项目支持手机、平板和 2in1 等多种设备形态，提供了完整的笔记管理、3D 交互体验和个性化主题定制能力。

| 项目信息 | 详情 |
|---------|------|
| 项目名称 | DayNote |
| 开发平台 | HarmonyOS 6.0.2 (API 12) |
| 开发语言 | ArkTS + C++ |
| 目标SDK | 22 |
| 支持设备 | 手机、平板、2in1 |
| 代码规模 | 90+ 个 ArkTS 源文件 |
| 架构模式 | 混合架构 (ArkTS 前端 + C++ 原生层) |

---

## 二、核心功能模块

### 2.1 智能笔记系统

#### 富文本编辑
- **多格式支持**: 加粗、斜体、下划线、文字颜色调整
- **图片管理**: 支持从相册选择图片插入笔记，可自定义图片尺寸（宽度 100-800px，高度 50-600px）
- **尺寸锁定**: 提供宽高比锁定功能，支持 1:1、4:3、16:9 等预设比例
- **实时预览**: 编辑内容实时渲染，支持多行文本显示

#### 笔记分类管理
- **双类型支持**: 
  - NOTE 类型：普通笔记，按日期组织
  - ARTICLE 类型：长篇文章，支持数据库同步
- **日期组织**: 按年/月/日分级目录存储，文件命名规则为 `note_{hour}_{min}_{sec}.json`
- **快速检索**: 支持按日期查询笔记列表，按时间倒序显示

#### 数据持久化
- **双重存储**: JSON 文件存储 + RdbStore 关系型数据库
- **自动清理**: 智能清理未使用的图片文件，节省存储空间
- **安全级别**: S3 级别数据库加密保护

### 2.2 3D 场景渲染系统

#### 场景管理
- **多场景支持**: 内置 6 种 3D 场景（主场景、照片墙、旋转球、碰撞演示等）
- **动态加载**: 支持运行时动态加载和切换场景资源
- **节点管理**: 支持添加 UI 平面、球体、卡片、图片等多种 3D 节点

#### 2D-3D 同步机制
- **UI 替身系统**: 将 2D UI 组件实时同步到 3D 场景中
- **位置映射**: 通过 UI ID → Node ID 映射表管理对应关系
- **实时同步**: 50 FPS 渲染循环，实时计算 2D UI 在 3D 空间中的位置和尺寸
- **视差倾斜**: 支持根据触摸位置计算 4x4 透视变换矩阵，实现真实的 3D 倾斜效果

#### 渲染特性
- **Shader 支持**: 自定义着色器实现特殊视觉效果
- **灯光系统**: 多光源配置，支持动态灯光控制
- **相机系统**: 可配置相机参数（视野、距离等）

### 2.3 自研物理引擎

#### 核心物理模拟
- **刚体动力学**: 完整的质量、速度、加速度、角速度物理模拟
- **碰撞检测**: AABB 包围盒 + 球体碰撞检测算法
- **摩擦力系统**: 可配置的摩擦系数和弹性恢复系数
- **重力系统**: 支持自定义重力方向和强度

#### 传感器集成
- **方向传感器**: 实时获取设备倾斜角度（alpha/beta/gamma）
- **重力映射**: 将设备倾斜角度映射为物理引擎重力方向
- **采样频率**: 20ms 采样间隔，确保流畅的物理交互体验

#### 事件系统
- **双缓冲队列**: 无锁并发的读写队列设计，避免线程竞争
- **三级优先级**: HIGH（触摸输入）、NORMAL（普通事件）、LOW（动画脚本）
- **事件类型**: 触摸、手势、射线检测、属性设置等 15+ 种事件类型

### 2.4 主题与响应式系统

#### 主题管理
- **多色板方案**: 提供 5 种预设色板（Default/Ocean/Forest/Sunset/Berry）
- **深色模式**: 支持 LIGHT/DARK/SYSTEM 三种模式
- **36 色令牌系统**: 完整的主色调、次色调、强调色、功能色、背景色定义
- **配置导出**: 支持主题配置导出和导入

#### 响应式设计
- **设备适配**: 自动检测 PHONE/TABLET/DESKTOP 设备类型
- **断点系统**: 
  - COMPACT (<600vp): 4 列网格
  - MEDIUM (600-840vp): 8 列网格
  - EXPANDED (>840vp): 12 列网格
- **动态字体**: 根据设备类型自动调整字体大小
- **间距系统**: 8 级间距令牌（4px-64px）

#### 组件尺寸规范
- **按钮系统**: 3 种高度（32/40/48px）
- **卡片系统**: 圆角 12px，2 种内边距（12/16px）
- **图标系统**: 4 种尺寸（12/16/20/24px）
- **字体系统**: 5 级字号（10/12/14/16/20px），4 种字重（300/400/500/700）

### 2.5 交互与动画系统

#### 页面导航
- **多页面叠加**: Main/Photo/Article 三大页面可左右滑动切换
- **弹性滚动**: 基于 PhysicsScroller 的橡胶带效果页面切换
- **过渡动画**: 页面切换时的淡入淡出效果

#### 卡片交互
- **3D 倾斜**: 触摸卡片时根据手指位置计算倾斜矩阵
- **动态阴影**: 根据倾斜角度实时调整阴影偏移
- **上下文菜单**: 长按弹出编辑和删除选项
- **淡入淡出**: 卡片出现/消失时的平滑动画

#### 日期选择
- **滑动选择器**: 7 天日期循环显示，支持左右滑动切换
- **循环队列**: 智能管理日期项的添加和移除
- **视觉反馈**: 选中日期高亮显示

#### 3D 轮播
- **Banner 组件**: 3D 旋转轮播效果，支持自动播放
- **透视变换**: 每个轮播项独立计算 3D 变换矩阵
- **触摸交互**: 支持手动滑动切换

---

## 三、技术架构

### 3.1 整体架构

```
┌─────────────────────────────────────────┐
│          应用层 (Application)            │
│  Index → MainPage → NotePage            │
│  ArticlePage → DemoPage → TestPage      │
├─────────────────────────────────────────┤
│          业务层 (Business)               │
│  NoteViewModel │ NoteState │ EditorCtrl  │
│  ThemeManager │ DeviceAdapter            │
│  PhysicsScroller │ SlideTimeModel        │
├─────────────────────────────────────────┤
│          核心层 (Core)                   │
│  SceneProxy │ EventQueue │ PhysicsMgr   │
│  Math Utils │ Perspective Transform      │
├─────────────────────────────────────────┤
│          原生层 (Native)                 │
│  C++ Physics Engine (libphysics.so)     │
│  NAPI Binding │ 3D Renderer              │
├─────────────────────────────────────────┤
│          数据层 (Data)                   │
│  RdbStore │ JSON File System            │
└─────────────────────────────────────────┘
```

### 3.2 目录结构

```
DayNote/
├── entry/src/main/
│   ├── ets/                          # ArkTS 前端代码
│   │   ├── entryability/             # 应用入口
│   │   ├── pages/                    # 页面层 (9个页面)
│   │   ├── feature/                  # 功能层 (30+组件)
│   │   │   ├── note/                 # 笔记模块
│   │   │   ├── mainDisplay/          # 主显示区
│   │   │   ├── SlideList/            # 滑动日期选择器
│   │   │   ├── calendar/             # 日历组件
│   │   │   ├── banner/               # 横幅轮播
│   │   │   ├── sidebar/              # 侧边栏
│   │   │   ├── demo/                 # 演示页面
│   │   │   └── DataStorage/          # 数据存储
│   │   ├── core/                     # 核心层
│   │   │   ├── scene3D/              # 3D场景管理
│   │   │   └── math/                 # 数学工具
│   │   ├── theme/                    # 主题系统
│   │   │   ├── core/                 # 主题管理器
│   │   │   └── tokens/               # 设计令牌
│   │   └── shared/                   # 共享工具
│   │       └── utils/                # 工具函数
│   ├── cpp/                          # C++ 原生代码
│   │   ├── nativerender/             # 原生渲染
│   │   ├── physics/                  # 物理引擎
│   │   └── types/                    # 类型定义
│   ├── resources/                    # 资源文件
│   └── module.json5                  # 模块配置
└── AppScope/                         # 应用全局配置
```

### 3.3 关键技术栈

| 技术分类 | 使用的技术 |
|---------|-----------|
| 前端框架 | ArkTS (声明式 UI) |
| 3D 渲染 | ArkGraphics3D (Component3D) |
| 原生开发 | C++ + NAPI |
| 数据库 | RdbStore (关系型数据库) |
| 传感器 | sensor (方向传感器) |
| 图像处理 | PixelMap、ImageUtils |
| 动画系统 | Animator、pageTransition |
| 数学计算 | 四元数、矩阵运算、透视变换 |

---

## 四、核心技术亮点

### 4.1 ArkTS + C++ 混合架构
- **前端**: ArkTS 声明式 UI，提供流畅的用户交互体验
- **后端**: C++ 实现高性能物理引擎，通过 NAPI 绑定与 ArkTS 通信
- **优势**: 充分发挥两种语言的优势，UI 层开发效率高，计算层性能优异

### 4.2 双缓冲事件队列
- **问题**: 物理引擎需要高频更新，事件处理存在线程竞争
- **方案**: 实现双缓冲读写队列，交换机制避免锁竞争
- **效果**: 50 FPS 稳定渲染，事件零丢失

### 4.3 2D-3D 同步机制
- **创新点**: 将 2D UI 组件实时映射到 3D 场景中的替身节点
- **实现**: 
  1. 获取 3D Node ID
  2. 获取 3D 视口尺寸
  3. 计算 2D UI 在 3D 空间的归一化位置
  4. 通过 EventQueue 发送更新指令
- **应用**: 笔记卡片在 3D 场景中真实呈现

### 4.4 透视变换矩阵系统
- **完整实现**: 4x4 透视变换矩阵计算
- **支持效果**:
  - 视差倾斜 (Parallax Tilt)
  - 卡片翻转 (Card Flip)
  - 卡片倾斜 (Card Tilt)
  - 网格透视 (Grid Card with Parallax)
- **数学基础**: 欧拉角转四元数、矩阵乘法、透视除法

### 4.5 传感器驱动物理
- **集成方案**: 使用方向传感器获取设备倾斜角度
- **映射算法**: 
  - beta 轴 → X 轴重力
  - gamma 轴 → Y 轴重力
  - alpha 轴 → Z 轴重力
- **应用场景**: 倾斜设备控制物理场景中物体的运动方向

### 4.6 弹性物理滚动
- **物理模型**: 
  - 橡胶带系数 (rubberK = 60)
  - 摩擦力 (friction = 0.0025)
  - 弹簧系数 (springK = 0.02)
  - 阻尼系数 (springDamping = 0.15)
- **效果**: 页面切换时的弹性回弹和阻尼振荡

### 4.7 富文本持久化
- **数据模型**: Span 级别的富文本序列化
- **支持格式**: 字体大小、颜色、粗细、样式、对齐方式
- **图片管理**: 本地路径存储 + 尺寸信息持久化
- **重建机制**: 从 JSON 完整重建富文本内容

---

## 五、功能演示

### 5.1 笔记管理
1. **创建笔记**: 点击底部新建按钮，输入标题和内容
2. **插入图片**: 通过工具栏图片按钮从相册选择
3. **格式调整**: 选中文本后应用加粗/斜体/下划线/颜色
4. **保存笔记**: 点击保存按钮，自动按日期归档
5. **浏览笔记**: 在主页面按日期查看笔记卡片列表
6. **编辑删除**: 长按卡片弹出上下文菜单

### 5.2 3D 交互
1. **场景加载**: 选择不同场景（照片墙、旋转球、碰撞演示）
2. **触摸旋转**: 手指滑动旋转 3D 场景
3. **射线检测**: 点击 3D 场景中的物体进行交互
4. **物理模拟**: 观察刚体碰撞、重力效果
5. **传感器控制**: 倾斜设备改变重力方向

### 5.3 主题定制
1. **切换色板**: 选择 Default/Ocean/Forest/Sunset/Berry
2. **深色模式**: 切换 LIGHT/DARK/SYSTEM 模式
3. **设备适配**: 自动根据设备类型调整布局

---

## 六、项目特色与创新

### 6.1 创新性
- **3D 笔记体验**: 将传统笔记应用与 3D 场景结合，提供沉浸式阅读体验
- **物理交互**: 利用物理引擎实现真实的弹性、碰撞效果
- **传感器融合**: 设备倾斜角度驱动物理模拟，增强交互感

### 6.2 技术深度
- **自研物理引擎**: 不依赖第三方库，完全自主实现刚体动力学
- **双缓冲架构**: 无锁并发设计，保证高频更新的稳定性
- **数学工具链**: 完整的向量、矩阵、四元数、透视变换实现

### 6.3 工程规范
- **模块化设计**: 清晰的层次划分（页面层/业务层/核心层/原生层）
- **设计令牌系统**: 完整的颜色/间距/尺寸/字体规范
- **响应式适配**: 支持多设备形态，自动调整布局

### 6.4 用户体验
- **流畅动画**: 50 FPS 渲染循环，页面切换弹性效果
- **视觉反馈**: 卡片倾斜、动态阴影、淡入淡出
- **个性化**: 5 种色板 + 深色模式，满足不同审美需求

---

## 七、开发成果统计

| 类别 | 数量 | 说明 |
|------|------|------|
| 页面文件 | 9 | Index/MainPage/NotePage/ArticlePage 等 |
| 功能组件 | 30+ | 笔记卡片、日期选择器、日历、Banner 等 |
| 核心模块 | 12 | 场景管理、事件队列、物理引擎、数学工具等 |
| 主题令牌 | 36+ | 颜色/间距/尺寸/字体完整规范 |
| C++ 文件 | 若干 | 物理引擎核心实现 |
| 总代码量 | 90+ 文件 | ArkTS 源文件 |

---

## 八、技术难点与解决方案

### 8.1 2D-3D 坐标同步
**难点**: 2D UI 组件的屏幕坐标与 3D 场景的世界坐标不一致
**解决方案**: 
1. 获取 3D 视口尺寸和相机参数
2. 计算归一化设备坐标 (NDC)
3. 通过透视矩阵转换到 3D 空间
4. 实时更新替身节点的位置和缩放

### 8.2 高频物理更新
**难点**: 物理引擎需要 50+ FPS 更新频率，事件处理易出现竞争
**解决方案**:
1. 实现双缓冲读写队列
2. 交换机制替代锁机制
3. 三级优先级保证关键事件优先处理

### 8.3 透视变换计算
**难点**: 卡片倾斜效果需要精确的透视投影计算
**解决方案**:
1. 实现完整的 4x4 矩阵运算库
2. 欧拉角转四元数避免万向节死锁
3. 视差倾斜算法计算每个卡片的独立变换

### 8.4 富文本持久化
**难点**: 富文本格式复杂，包含多种样式和图片
**解决方案**:
1. Span 级别的数据模型设计
2. JSON 序列化/反序列化完整保留样式
3. 图片路径本地化存储

---

## 九、未来扩展方向

1. **云同步**: 集成 HarmonyOS 云空间，实现多设备笔记同步
2. **AI 助手**: 接入大语言模型，提供智能摘要、翻译、改写功能
3. **协作编辑**: 支持多人实时协作编辑同一笔记
4. **插件系统**: 开放插件接口，支持第三方扩展
5. **AR 模式**: 利用 AR 引擎将笔记内容投射到现实场景

---

## 十、总结

DayNote 是一款功能完整、技术先进的 HarmonyOS 笔记应用。项目不仅实现了基础的笔记管理功能，还创新性地集成了 3D 场景渲染、自研物理引擎、传感器驱动等高级特性。通过 ArkTS + C++ 混合架构，充分发挥了两种语言的优势，实现了高性能与高开发效率的平衡。

项目的技术亮点包括：
- ✅ 完整的 3D 场景与物理引擎集成
- ✅ 创新的 2D-3D 同步机制
- ✅ 自研双缓冲事件队列架构
- ✅ 完善的主题与响应式系统
- ✅ 丰富的交互动画效果

DayNote 展示了 HarmonyOS 平台的应用开发能力，为移动笔记应用提供了新的可能性。

---

**文档版本**: v1.0  
**生成日期**: 2026年5月5日  
**项目名称**: DayNote 智能笔记系统  
**开发平台**: HarmonyOS 6.0.2
